#!/bin/sh
# Fetch the Mathlib cache when a fresh worktree is created.
# $1 = previous HEAD; git passes all zeros for a new worktree or clone.
if [ "$1" = "0000000000000000000000000000000000000000" ]; then
  lake exe cache get
fi
